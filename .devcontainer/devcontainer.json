{
  "name": "weatherapp",
  "dockerComposeFile": "docker-compose.yml",
  "service": "app",
  "workspaceFolder": "/workspace",
  "remoteEnv": {
    "REDIS_URL": "redis://redis:6379",
    "DATABASE_URL": "postgresql://weatherapp:weatherapp@postgres:5432/weatherapp"
  },
  "customizations": {
    "vscode": {
      "extensions": ["dbaeumer.vscode-eslint", "esbenp.prettier-vscode", "vitest.explorer"],
      "settings": {
        "editor.formatOnSave": true,
        "editor.defaultFormatter": "esbenp.prettier-vscode",
        "editor.codeActionsOnSave": {
          "source.fixAll.eslint": "explicit"
        }
      }
    }
  },
  "postCreateCommand": "corepack enable && pnpm install && pnpm db:migrate",
  "forwardPorts": [3000]
}
